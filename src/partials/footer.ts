/**
 * Footer Partial
 * Format: Markdown divider + tagline
 * Use: Every system tool — consistent branding
 *
 * Design principles:
 * 1. One source — single place to update branding
 * 2. Minimal — divider + one line
 * 3. Link included — points to repo
 * 4. Tagline — memorable identifier
 */

/**
 * Standard footer for all FloatPrompt tools
 */
export const footer = (): string =>
  `---

*[FloatPrompt](https://github.com/mds/floatprompt) — the invisible OS for AI*`;
