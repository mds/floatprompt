# FloatPrompt Human Essential Fields
# Basic human context and preferences
# Source: atomic component extracted from header.yaml 
human:
  intent:
    primary: "{{HUMAN_INTENT_PRIMARY}}"
    constraints: "{{HUMAN_INTENT_CONSTRAINTS}}"
  preferences:
    tone_drift_allowed: false
    verbosity: "{{HUMAN_VERBOSITY}}"
    allow_ai_suggestions: true 