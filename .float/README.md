# STALE - Do Not Use

This `.float/` folder uses the **OLD structure** from a previous iteration.

**For current work, see:**
- `.float-manual/boot.md` — THE context setter for FloatPrompt development
- `.float-manual/` — Manual prototype of the new structure

**What's old here:**
- `float.md`, `project.md` at root level
- `nav/`, `context/`, `logs/` as separate folders
- `tools/` with old tool definitions

**The NEW structure** (documented in `.float-manual/boot.md`):
- `_/` convention — each folder gets `_/` containing map.md, context.md, logs/
- `.float/project/` mirrors project folder structure
- Agents maintain automatically

This folder will be deleted or replaced once the new structure is implemented.
